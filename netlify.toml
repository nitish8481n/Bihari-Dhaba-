[build]
  publish = "."
  functions = "netlify/functions"

# Lets the website call "/api/chat" — Netlify quietly routes it to the function.
[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
